version: '3.8'

services:
  traffic_forecast:
    build: ./backend
    ports:
      - 5000:5000
    environment:
      - SECRET_KEY=
    volumes:
      - ./backend:/app
    command: uvicorn src.main:app --reload --host 0.0.0.0 --port 5000
